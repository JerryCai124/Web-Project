using System;
using System.Collections.Generic;
using System.Linq;
using System.Security.Cryptography;
using System.Text;
using System.Web;

namespace Lab1_3050.BL
{
    public class BL_MD5
    {
       // public static string MD5(string str)
       // {
             //创建MD5对象
           //  MD5 md5 = MD5.Create();
               //将数据转换成字节数组
            //   byte[] bytes = Encoding.UTF8.GetByte(str);
              // 对字节数组进行加密
            //  byte[] bytes2 = md5.ComputeHash(bytes);
            //将字节数组转换成十六进制的字符串
            //1.创建stringBuilder对象
          //  System.Text.StringBuilder st = new StringBuilder();
           //  for (int i = 0; i < bytes2.Length; i++)
           //               {
           //    st.AppendFormat(bytes2[i].ToString("x2"));
           //               }
          //     return st.ToString();
          //  }
   }
}